apiVersion: v1
kind: Service
metadata:
  name: immich-server
  namespace: immich
spec:
  type: NodePort
  ports:
  - port: 2283
    targetPort: 3001
    protocol: TCP
  selector:
    app: immich-server
  type: LoadBalancer
  loadBalancerIP: 10.100.99.85